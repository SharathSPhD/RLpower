"""Integration tests: require --run-integration flag and compiled FMU.

These tests are SKIPPED by default. Run with:
    pytest --run-integration tests/integration/

Must run inside Docker on DGX Spark (RULE-D1, RULE-D2).
"""
